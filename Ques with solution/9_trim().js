//9. Use trim() to remove any trailing whitespace at the beginning and the end of a string.
let fullName = "   Arpit Pathak             ";
let trimmedName = fullName.trim();
console.log(fullName);
console.log(trimmedName);

// cheking lengths of both variables
console.log("full name space  " + fullName.length);
console.log("trimmed name space  " + trimmedName.length);